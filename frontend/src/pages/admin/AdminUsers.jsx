import { useEffect, useState } from 'react';import PageHeader from '../../components/PageHeader';import AdminTabs from '../../components/AdminTabs';import api from '../../services/api'
export default function AdminUsers(){const [rows,setRows]=useState([]);const load=()=>api.get('/admin/users').then(r=>setRows(r.data));useEffect(()=>{load()},[]);const toggle=async id=>{await api.patch(`/admin/users/${id}/toggle`);load()};return <div className="container py-5"><PageHeader eyebrow="Administration" title="Learners and access" subtitle="Review registered accounts and control user access."/><AdminTabs/><div className="table-responsive panel p-0 mt-4"><table className="table align-middle mb-0"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td><strong>{u.fullName}</strong></td><td>{u.email}</td><td>{u.role}</td><td><span className={`result-pill ${u.enabled?'pass':'fail'}`}>{u.enabled?'Active':'Disabled'}</span></td><td>{u.role!=='ADMIN'&&<button className="btn btn-sm btn-outline-dark" onClick={()=>toggle(u.id)}>{u.enabled?'Disable':'Enable'}</button>}</td></tr>)}</tbody></table></div></div>}
