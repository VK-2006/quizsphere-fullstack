import { useEffect, useState } from 'react';import { Link } from 'react-router-dom';import PageHeader from '../../components/PageHeader';import AdminTabs from '../../components/AdminTabs';import api,{errorMessage} from '../../services/api'
const blank={title:'',description:'',difficulty:'EASY',durationMinutes:10,passPercentage:60,categoryId:'',published:false}
export default function AdminQuizzes(){const [rows,setRows]=useState([]),[cats,setCats]=useState([]),[form,setForm]=useState(blank),[edit,setEdit]=useState(null),[msg,setMsg]=useState('');const load=()=>Promise.all([api.get('/admin/quizzes'),api.get('/categories')]).then(([q,c])=>{setRows(q.data);setCats(c.data)});useEffect(()=>{load()},[]);const save=async e=>{e.preventDefault();setMsg('');const payload={...form,categoryId:Number(form.categoryId),durationMinutes:Number(form.durationMinutes),passPercentage:Number(form.passPercentage)};try{edit?await api.put(`/admin/quizzes/${edit}`,payload):await api.post('/admin/quizzes',payload);setForm(blank);setEdit(null);load()}catch(e){setMsg(errorMessage(e))}};const choose=q=>{setEdit(q.id);setForm({title:q.title,description:q.description||'',difficulty:q.difficulty,durationMinutes:q.durationMinutes,passPercentage:q.passPercentage,categoryId:q.categoryId,published:q.published})};const remove=async id=>{if(confirm('Delete this quiz and its questions?')){try{await api.delete(`/admin/quizzes/${id}`);load()}catch(e){setMsg(errorMessage(e))}}};return <div className="container py-5"><PageHeader eyebrow="Administration" title="Quiz studio" subtitle="Build, publish, and maintain interactive assessments."/><AdminTabs/>{msg&&<div className="alert alert-danger mt-3">{msg}</div>}<div className="row g-4 mt-1"><div className="col-xl-5"><form className="panel" onSubmit={save}><h3>{edit?'Edit':'Create'} quiz</h3><label className="form-label">Title</label><input className="form-control mb-3" required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><label className="form-label">Description</label><textarea className="form-control mb-3" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><div className="row"><div className="col"><label className="form-label">Category</label><select className="form-select mb-3" required value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value})}><option value="">Select</option>{cats.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></div><div className="col"><label className="form-label">Difficulty</label><select className="form-select" value={form.difficulty} onChange={e=>setForm({...form,difficulty:e.target.value})}>{['EASY','MEDIUM','HARD'].map(x=><option key={x}>{x}</option>)}</select></div></div><div className="row"><div className="col"><label className="form-label">Minutes</label><input className="form-control" type="number" min="1" value={form.durationMinutes} onChange={e=>setForm({...form,durationMinutes:e.target.value})}/></div><div className="col"><label className="form-label">Pass %</label><input className="form-control" type="number" min="1" max="100" value={form.passPercentage} onChange={e=>setForm({...form,passPercentage:e.target.value})}/></div></div><div className="form-check form-switch my-4"><input className="form-check-input" type="checkbox" checked={form.published} onChange={e=>setForm({...form,published:e.target.checked})}/><label className="form-check-label">Published</label></div><button className="btn btn-primary">{edit?'Update':'Create'} quiz</button>{edit&&<button type="button" className="btn btn-link" onClick={()=>{setEdit(null);setForm(blank)}}>Cancel</button>}</form></div><div className="col-xl-7"><div className="panel"><h3>All quizzes</h3>{rows.map(q=><div className="admin-list-row quiz-admin-row" key={q.id}><div><strong>{q.title}</strong><small>{q.categoryName} · {q.questionCount} questions · {q.published?'Published':'Draft'}</small></div><div className="d-flex gap-1"><Link className="icon-btn" title="Questions" to={`/admin/quizzes/${q.id}/questions`}><i className="bi bi-list-check"/></Link><button className="icon-btn" onClick={()=>choose(q)}><i className="bi bi-pencil"/></button><button className="icon-btn danger" onClick={()=>remove(q.id)}><i className="bi bi-trash"/></button></div></div>)}</div></div></div></div>}
